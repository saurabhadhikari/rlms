<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="stream.list">
      <field name="name">stream list</field>
      <field name="model">stream.stream</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="stream.action_window">
      <field name="name">stream window</field>
      <field name="res_model">stream.stream</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="stream.action_server">
      <field name="name">stream server</field>
      <field name="model_id" ref="model_stream_stream"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="stream" id="stream.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="stream.menu_1" parent="stream.menu_root"/>
    <menuitem name="Menu 2" id="stream.menu_2" parent="stream.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="stream.menu_1_list" parent="stream.menu_1"
              action="stream.action_window"/>
    <menuitem name="Server to list" id="stream" parent="stream.menu_2"
              action="stream.action_server"/>
    -->
  </data>
</odoo>